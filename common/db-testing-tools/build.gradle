plugins {
    id 'kotlin'
}

dependencies {
    implementation("javax.inject:javax.inject:1")

    testImplementation testLibs.junitCore
    testImplementation testLibs.junitJupiter
    testImplementation testLibs.mockito
    testImplementation testLibs.coroutines
}